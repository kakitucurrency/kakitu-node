
# Bind address for the RPC server
# type:string,ip
address = "::ffff:0.0.0.0"

# RPC server port (overrides network default so the same port works for dev and live)
# type:uint16
port = 44076

# Enable or disable control-level requests
# type:bool
enable_control = true
